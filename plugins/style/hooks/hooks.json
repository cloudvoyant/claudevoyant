{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "INPUT=$(cat); FILE=$(echo \"$INPUT\" | jq -r '.tool_input.file_path // empty'); if [[ \"$FILE\" == *\".style/\"* ]] && [[ \"$FILE\" == *.json ]]; then if ! jq empty \"$FILE\" 2>/dev/null; then echo \"⚠️  $FILE is invalid JSON after write — review manually\" >&2; fi; fi",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
